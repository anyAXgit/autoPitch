# 변경 기록

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은
[유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [Unreleased]

### 추가
- 첫 화면에서 `ffmpeg 설치하기` 버튼 — 각 OS 패키지 관리자로 한 번에 설치.
- 설정 화면의 세부 설정 10종 — `config.yaml` 을 열지 않고 탐지 민감도·장면
  길이·출력 크기 등을 조절. 저장해도 파일의 주석과 정렬은 그대로 유지된다.
- 렌더 남은 시간 표시, 완료 후 `결과 폴더 열기`.
- 단축키 패널 (`?`).

### 변경
- 오픈소스 공개 정리: 개인 데이터(`net_rois.json`, `data/train_events.jsonl`)를
  추적에서 제외하고 예시 파일로 대체. AGPL 의존성(ultralytics)을 선택 extra 로
  분리하고 해당 모듈에 라이선스 경계를 명시.

### 보안
- 로컬 서버가 자기 페이지에서 온 요청만 처리한다. 이전에는 `127.0.0.1` 바인드에
  기대고 있었는데, 그건 다른 기계를 막을 뿐 다른 **탭**을 막지 못한다. 앱이 켜져
  있는 동안 사용자가 방문한 아무 사이트나 `/api/install_ffmpeg`(패키지 관리자
  실행)나 `/api/settings`(설정 덮어쓰기)를 호출할 수 있었고, DNS 리바인딩을 쓰면
  응답까지 읽혔다. `Host`·`Origin`·`Sec-Fetch-Site` 세 헤더를 확인해 막는다.
  두 헤더가 모두 없는 요청(curl, 스크립트)은 그대로 통과 — 웹페이지는 브라우저가
  이 헤더를 빼도록 만들 수 없다.

### 수정
- 원본을 옮긴 뒤 렌더하면 ffmpeg 명령줄이 그대로 노출되던 오류 → 원본을 먼저
  확인하고 어떤 파일이 없는지 안내.
- 골대 위치 탭이 저장된 ROI 를 "아직 안 그림" 으로 표시하던 문제 (파이프라인은
  파일명으로 찾고 있었는데 화면만 정확한 경로로 보고 있었다).
- 영상이 든 폴더가 따로 있는데도 빈 폴더를 기본 선택하던 문제.

## [0.1.1] - 2026-07-29

### 추가
- 첫 실행 환경 점검 — ffmpeg, 필수 필터, 하드웨어 인코더, 폴더 권한, 디스크.
- 영상 폴더 열기, 진행 흐름 안내.

### 변경
- 전문 용어 정리 (`골 ROI` → `골대 위치`, `in/out/컷` → `시작/끝/전환` 등).

### 수정
- Windows 콘솔(cp949)에서 앱이 첫 출력 줄에 죽던 문제.

## [0.1.0] - 2026-07-29

첫 설치판 (macOS / Windows).

### 추가
- macOS `.app` / Windows `.exe` 빌드와 GitHub Actions 자동 빌드·릴리스.
- 3개 OS 에서 테스트를 도는 CI.

### 변경
- `librosa` 제거 — 오디오 디코딩·RMS 를 ffmpeg + numpy 로 직접 처리. 결과는
  동일(피크 시각 일치, dB 오차 5e-6 이하)하고 디코딩은 약 12배 빠르다.
  numba/llvmlite/sklearn 등 약 215MB 의존성이 빠졌다.

### 수정
- Windows 에서 concat 경로의 백슬래시가 이스케이프로 해석되어 렌더가 실패하던 문제.
- 텍스트 파일 입출력의 인코딩 미지정 (한국어 Windows 에서 cp949 로 깨짐).
- 하드웨어 인코더가 videotoolbox 만 지원되던 문제 → nvenc/qsv/amf 추가.
