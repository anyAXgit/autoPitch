# 보안 취약점 신고

## 신고 방법

**공개 이슈로 올리지 말아 주세요.** 저장소의 **Security** 탭 → **Report a vulnerability**
로 비공개 신고를 보내주시면 됩니다. 저만 볼 수 있고, 고친 뒤에 함께 공개할 수 있습니다.

1인 프로젝트라 24시간 대응 같은 건 약속드릴 수 없습니다. 대신 이 정도는 지키겠습니다.

- **일주일 안에** 받았다는 답을 드립니다.
- 재현이 되면 무엇이 문제이고 언제 고칠지 알려드립니다.
- 고치면 릴리스 노트에 적고, 원하시면 신고자로 이름을 남깁니다.

## 지원 버전

가장 최근 릴리스만 고칩니다. 이전 버전으로 되돌아가 패치하지는 않습니다.

## 무엇이 취약점인가

이 프로그램은 **내 컴퓨터에서 도는 로컬 도구**입니다. 서버도 계정도 없고, 촬영본은
어디로도 올라가지 않습니다. 그래서 신경 쓸 곳이 좁습니다.

**보고해 주시면 좋은 것:**

- **로컬 서버**(`gui/server.py`)가 자기 페이지가 아닌 곳의 요청을 처리하는 경우.
  `127.0.0.1` 에만 붙지만 그건 다른 기계를 막을 뿐 브라우저의 다른 탭을 못 막습니다.
  `Host`·`Origin`·`Sec-Fetch-Site` 를 확인해 거르는데, 이걸 우회하는 방법이 있다면
  그건 취약점입니다.
- **경로 처리.** 사용자가 열어준 폴더 밖의 파일을 읽거나 쓰게 만들 수 있는 경우.
- **API 키 취급.** `data/_gui/anthropic_key.txt` 의 키가 로그·오류 메시지·`plan.json`
  같은 산출물에 새어 나오는 경우.
- **배포판**(`.app`/`.exe`)이 서명을 우회하거나, 예상치 못한 코드를 실행하는 경우.

**여기서 다루지 않는 것:**

- **ffmpeg 자체의 취약점** — 별도 프로세스로 호출할 뿐이라 업스트림에 신고해 주세요.
- **이미 그 컴퓨터를 쓸 수 있는 사람**이 할 수 있는 일. 로컬 도구라 사용자와 같은
  권한으로 돕니다.
- **선택 확장**(`ultralytics` 등)에서 오는 문제. 직접 설치해서 쓰는 영역입니다.
- 사용자가 스스로 자기 파일을 지우거나 덮어쓰는 동작.

## 이 프로젝트가 다루는 데이터

신고 내용을 판단하는 데 도움이 되도록 적어둡니다.

| | |
|---|---|
| 촬영본 | 읽기만 합니다. 복사하지도, 어디로 보내지도 않습니다 |
| 골 라벨링(선택) | 켰을 때만, 후보 장면의 **정지 프레임 몇 장**이 Anthropic API 로 갑니다 |
| API 키 | `data/_gui/anthropic_key.txt` 에서 읽어 환경변수로만 넘깁니다. 기록하지 않습니다 |
| 그 외 | 네트워크로 나가는 것이 없습니다 |
