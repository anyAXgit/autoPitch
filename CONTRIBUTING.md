# 기여

이 프로젝트는 특정 조건에서 시작했습니다 — 코트 코너에 고정한 카메라 두 대,
야간 실내 풋살. 다른 환경에서 돌려본 결과가 가장 반가운 기여입니다.

## 개발 환경

```bash
python3 -m venv .venv
./.venv/bin/pip install -r requirements-dev.txt
./.venv/bin/python -m pytest -q      # ffmpeg 필요
```

`requirements-dev.txt` 는 코어 의존성에 테스트·빌드용을 더한 것입니다.
`requirements-optional.txt` 는 **AGPL-3.0** 인 ultralytics 를 담고 있고,
보류된 실험 모듈에서만 씁니다 — 설치하지 않아도 전부 동작합니다.

## 원칙

**측정하지 않은 것은 주장하지 않습니다.** 이 프로젝트에서 그럴듯해 보였지만
실제로 죽은 접근이 여럿 있습니다(모션 통계로 골 판별, 골대 안 공 탐지 — 알고 보니
그 구장은 여분 공을 골대 뒤에 두고 있었습니다). 성능이나 정확도를 바꾸는 변경은
**어떤 영상에서 무엇을 쟀는지** 함께 적어 주세요. 실패한 시도도 그 기록이 남아
있으면 다음 사람이 같은 길을 다시 걷지 않습니다.

주석은 *무엇* 이 아니라 *왜* 를 씁니다. 코드가 이상해 보이는 데는 대개 이유가
있고(`-loop 1` 이 왜 필요한지, ROI 검색창이 왜 온셋보다 앞쪽인지), 그 이유가
사라지면 다음 사람이 "정리" 하다가 되살립니다.

## 테스트

`tests/` 는 ffmpeg 로 합성한 짧은 영상을 씁니다 — 실제 촬영본은 저장소에
들어가지 않습니다. 새 기능에는 테스트를 붙여 주세요. CI 는 Linux·macOS·Windows
에서 돕니다. 경로·인코딩 관련 변경은 Windows 결과를 꼭 확인하세요. 지금까지
나온 이식성 버그 두 개 모두 Windows 에서만 드러났습니다.

## 커밋

무엇을 왜 바꿨는지 씁니다. 측정값이 있으면 본문에 넣어 주세요.

## 저장소에 넣지 않는 것

촬영 원본, API 키, `net_rois.json`(파일명이 그대로 들어갑니다),
`data/train_events.jsonl`. 예시가 필요하면 `*.example.*` 를 참고하세요.
