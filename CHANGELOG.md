# 변경 기록

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은
[유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [Unreleased]

### 수정
- **직접 편집한 클립이 다시 열 때마다 복제되던 문제.** 편집 병합에서 손댄 클립은
  그대로 두는데, 그 클립이 **탐지 쪽 짝을 소비하지 않아서** 같은 함성의 탐지 결과가
  "새 후보"로 또 붙었다. 직접 편집한 클립 하나당 복제본 하나가 생겼다 — 실측한 두
  경기에서 42개·69개짜리 플랜이 74개·72개로 불어났고, 그중 17개·16개는 복제본까지
  '포함' 상태라 **렌더 결과에 같은 장면이 두 번 들어갔다**. 이제 손댄 클립도 짝을
  소비한다. 이미 불어난 편집 파일은 앱을 다시 열면 그대로 남으므로, 중복은
  인스펙터에서 제외하거나 지우면 된다.

## [0.3.1] - 2026-08-16

### 변경
- **렌더 화질.** 잔디는 화면 전체가 잘게 무늬진 데다 빠르게 움직여서 H.264 에
  가장 불리한 그림인데, 두 가지가 겹쳐 뭉개지고 있었다.
  - 마지막에 클립들을 이어 붙이면서 **전체를 다시 인코딩**하고 있었다. 모든 클립이
    같은 인코더·같은 크기·같은 프레임레이트로 나왔으니 픽셀을 건드릴 이유가 없다.
    이제 그대로 복사한다 — 한 세대분의 손실이 통째로 사라지고, 렌더 시간도 준다.
  - 하드웨어 인코딩(VideoToolbox 등)을 **기본에서 껐다.** 빠르지만 같은 비트를 훨씬
    못 쓴다. 1080p30 한 클립을 원본과 대조한 SSIM: 하드웨어 10Mbps 0.9563 ·
    20Mbps 0.9806 · 30Mbps 0.9890 인데, `x264 -crf 20` 은 **17Mbps 에서 0.9907** 로
    비트레이트를 두 배 가까이 준 하드웨어보다 좋았다. 대신 2.6배 느리다(8초 클립에
    3.7초 대 1.4초). 급할 때는 `hw_encode: true` 로 켜면 되고, 그 경우 비트레이트를
    10M → 25M 으로 올려 두었다.
  - 실측 결과(같은 클립, 원본 대조 SSIM): 예전 파이프라인 **0.9464** → 지금 기본
    **0.9630**. 움직임이 많은 구간에서는 차이가 더 크다 — 0.9439 → 0.9907.
  - `video_crf`(기본 20)와 `video_preset`(기본 medium)을 설정과 설정 화면에 노출했다.

### 수정
- **인코딩 설정을 렌더 시점의 설정에서 읽는다.** 플랜을 만들 때 그 시점 설정을 같이
  저장하고 렌더가 그걸 읽고 있었다. 그래서 새 버전으로 올려도 기존 프로젝트를
  렌더하면 예전 인코딩을 그대로 탔고, 화질 설정을 바꾸려면 골 탐지를 처음부터 다시
  돌려야 반영됐다. 인코딩은 탐지가 아니라 렌더에 속한 설정이다.

## [0.3.0] - 2026-08-16

### 변경
- **어느 카메라를 보여줄지는 소리가 정한다.** 골망 ROI 는 '언제'만 보정한다.
  두 ROI 는 같은 것을 재고 있지 않다 — 카메라마다 자기 옆 골대가 화면을 채우므로,
  그 앞에 선 키퍼의 움직임이 먼 골망에 공이 들어가는 것보다 항상 크게 잡힌다.
  실제로 눈으로 확인한 5건에서 가까운 골대 쪽 카메라가 모션은 매번 이기고 매번
  틀렸으며(9:55 은 10.0 대 5.2, 10:28 은 12.3 대 3.6), 소리는 5건 다 맞혔다.
  `locate.angle_from_roi` 로 되돌릴 수 있다 — 키퍼가 빠지도록 ROI 를 좁게 잡은
  설치라면 켜는 편이 나을 수 있다.

  실측한 두 경기에서 앵글이 바뀌는 클립은 20~23%(15개 / 13개)다. 나머지는 원래
  소리로 정해지던 것이라 변화가 없다.

  **알려진 한계 — 소리는 약한 신호다.** 두 카메라의 판정 차이는 중앙값 0.3~0.4k 에
  불과하고, 네 경기에서 10~30% 의 클립은 차이가 0.15 미만이라 사실상 동점이다.
  경기4 17:05 은 0.02 차이로 갈렸고 틀렸다. 대안을 다섯 가지 재봤지만 — 화면 전체
  움직임, 움직임의 세로 무게중심, 움직이는 덩어리 크기, 자기 골망 ROI 모션(raw 및
  자기 분포 정규화) — 눈으로 확인한 사례에서 전부 우연 수준이었고, 신호끼리 같은
  답을 내는 비율도 42개 중 20~25개였다. 값싼 화면 통계로는 "공이 어느 쪽에 있나"를
  못 맞힌다. 제대로 하려면 선수나 공을 실제로 찾아야 한다.
  (부수 확인: ROI 모션을 자기 분포로 정규화하면 편향은 실제로 없어진다 — raw 로는
  cam1 을 42개 중 12번만 고르는데 정규화하면 22번이다. 균형은 맞지만 정확하진 않다.)
  당장은 **앵글이 틀리면 인스펙터에서 한 번에 바꿀 수 있다**는 점에 기대야 한다.

### 성능
- **골망 ROI 확인이 절반 가까이 빨라졌다.** 실측한 경기에서 `build_plan` 82.6초 →
  47.0초(1.76배)이고, 결과는 완전히 같다 — 클립 66개, 앵커·앵글 전부 일치.
  - ROI 탐색을 **동시에** 돌린다. 탐색은 저마다 자기 구간으로 이동하는 별개의
    ffmpeg 프로세스라 서로 기다릴 이유가 없었다. 다만 ffmpeg 이 이미 자기 디코딩을
    멀티스레드로 하기 때문에 코어 수만큼 빨라지지는 않는다(10코어에서 1.7배).
    `locate.workers`(기본 6).
  - **약한 오디오 후보 탐색에 상한**을 뒀다(`locate.weak_audio_max_probes`, 기본 20).
    후보 하나 건지는 데 가장 비싼 단계다 — 69번 탐색에 40초를 쓰고 채택은 1개였고,
    그 1개가 69개 중 소리 1위였다. 소리 큰 순으로 상위 N 개만 본다. 0 이면 전부.
  - 시도했다가 버린 것: **하드웨어 디코딩**은 카메라마다 반대로 나왔다(cam1 435→317ms,
    cam2 502→886ms). **한 번에 전체 통과**는 3~5배 느렸다(전체 159초 대 구간 66개 30초)
    — 지금의 구간 탐색 방식이 이미 맞다.
- 진행 표시가 **경기 어디를 보고 있는지** 알려준다 (`ROI 골망 모션 확인 45/132 · cam2 12:30`).

### 추가
- **분석 중에 무엇을 보고 있는지 보여준다.** 진행 패널에 경기 전체 길이의 막대가
  뜨고, 지금 확인 중인 위치가 움직이며 골망 히트가 눈금으로 쌓인다. 옆에는 지금
  모션을 재고 있는 **골망 ROI 조각**과, 히트가 잡힌 **장면 스틸**이 뜬다.
  ROI 조각은 모션 점수를 내려고 어차피 디코딩한 픽셀이라 비용이 0이고 — 골키퍼가
  상자 안에 서 있는 것 같은 잘못 그린 ROI 가 숫자가 아니라 그림으로 보인다.
  스틸은 히트한 것만 뽑는다(3번에 1번 꼴). 탐색마다 뽑으면 방금 줄인 시간을
  그대로 되돌려 놓기 때문이다.

### 수정
- **클립이 자기 함성보다 먼저 끝나던 문제.** 골망 ROI 가 앵커를 골 프레임으로
  끌어당기면 앵커와 함성 사이가 벌어지는데, 그 사이는 당연히 조용하다.
  "가라앉았는지"를 앵커부터 찾고 있어서 그 조용한 구간을 끝으로 잡았다 — 함성이
  터지기도 전에, 즉 골이 나오기도 전에 클립이 닫혔다. 이제 함성 이후부터 찾는다.
  실측한 세 경기에서 함성이 클립 끝에 1초도 안 남던 클립이 24% · 24% · 26% 였고,
  최악은 함성보다 6.4초 앞서 끝났다. 경기3 17:46 은 함성이 18:00 인데 클립이
  17:56 에 끝나 골 자체가 빠져 있었다 — 새 규칙은 18:02 로, 사용자가 직접 고른
  값과 같다. 사용자가 손으로 고친 23개와 대조하면 끝 위치 오차가 평균 4.0s →
  3.4s 이고, 6개 개선 · 14개 동일 · 3개 소폭 악화(최대 2.1s)다.
- 앵글 투표가 **클립 전체**를 듣는다. 이전에는 앵커부터 끝까지만 들었는데, 앵커는
  ROI 가 골 프레임으로 끌어당기는 값이라 창이 ROI 를 따라 움직였다 — 소리로 정하기로
  한 판단을 ROI 가 뒷문으로 되돌리고 있었던 셈이다. 실측한 경기 10:28 에서는 창이
  충분히 앞으로 밀려서 이 클립을 만든 함성(639.5초)이 창(633.6~638.6) 밖으로
  빠졌고, 결과적으로 잡음 바닥이 높은 카메라가 뽑혔다. 리액션 앵글은 축하 구간을
  보는 게 맞으므로 그대로 둔다. 이 경기에서 앵글이 바뀌는 클립은 11개(16%).
- **앵글 전환이 근거 없이 일어나던 문제.** 전환 대상은 "가장 크게 들은 나머지
  카메라"였는데, 카메라가 2대면 나머지가 하나뿐이라 사실상 판정이 아니었다 —
  조건만 맞으면 무조건 반대편으로 넘어갔다. 실측한 두 경기의 전환 4건 중 3건이
  **더 조용한 카메라로** 넘어갔고, 경기1 7:11 은 cam2 에서 함성이 +4.70 으로
  터지는 한복판에 +0.72 인 cam1 으로 넘어가고 있었다. 함성에서 등을 돌리는 셈이다.
  이제 전환 구간에도 주 앵글과 같은 질문을 한다 — 이 구간을 누가 들었나. 이미
  나오고 있는 카메라가 여전히 제일 크게 듣고 있으면 넘기지 않는다. 두 경기에서
  전환 4건 중 1건만 남고, 그 하나는 방향이 뒤집혔다(10:49 cam1→cam2 였던 것이
  cam2→cam1).

  전환이 걸리는 조건 자체는 원래도 소리였다 — 함성이 가라앉는 시점(`reaction_end`)이
  10.5초를 넘겨야 두 번째 앵글이 들어갈 자리가 생긴다. 세리머니를 눈으로 판독하지
  않는다. 131개 클립 중 전환이 있던 건 4개(3%)였고 이제 1개다.
- **저장된 편집이 새 탐지를 덮어쓰던 문제.** 저장 파일이 클립의 `segments` 까지
  통째로 들고 있어서, 다시 분석해도 화면에 뜨는 순간 지난번 카메라·타이밍으로
  덮이고 그게 다시 저장됐다. 앵글을 고쳐 배포해도 사용자가 영영 못 보는 상태였다.
  이제 **결정만** 남는다 — 포함/제외, 직접 자른 시간, 직접 고른 앵글, 복제, 분할.
  손대지 않은 클립은 새 탐지 결과를 받는다. 클립 짝짓기는 `peak`(그 클립을 만든
  함성)로 한다. 앵커 로직이 바뀌어도 움직이지 않는 유일한 값이기 때문이다.
  불러올 때 몇 개가 새 탐지를 받았고 몇 개가 직접 편집으로 남았는지 표시한다.

## [0.2.0] - 2026-08-16

### 변경
- **함성 탐지가 모든 카메라를 듣는다.** 이전에는 첫 번째 카메라 하나만 들었고,
  나머지 카메라 오디오는 싱크와 앵글 선택에만 썼다. 반대편 골에서 난 함성은
  가까운 카메라에 크게, 먼 카메라에 작게 잡히므로 — 실제 한 경기에서 cam2 에
  k=4.76 으로 남은 함성이 cam1 에서는 1.53 이라 후보로 오르지도 못했다.
  이제 프레임별로 가장 크게 들은 카메라를 쓴다. 카메라마다 게인·거리·잡음
  바닥이 달라 dB 는 비교할 수 없으므로, 각 녹음의 중앙값 대비 표준편차(k)로
  환산해 비교한다.
- `min_gap_sec` 15 → 6초. 풋살은 골이 연달아 터지는데 15초 클러스터당 하나만
  남기고 있었다. 실측한 경기에서 임계값을 통과한 장면 둘이 이 때문에 버려졌다.
- `threshold_k` 3.0 → 2.4.

  이 셋을 합치면 실측한 경기에서 사용자가 직접 찾아 넣어야 했던 5개가 전부 후보로
  올라온다. 대신 후보가 23 → 71개로 늘어난다. 놓친 장면은 복구할 방법이 없고 남는
  후보는 클릭 한 번이라, 이 방향을 기본값으로 삼았다.
- **골망 ROI 가 답을 못 냈을 때 주 앵글을 소리로 고른다.** 이전에는 무조건 첫
  카메라였다 — 실측한 경기에서 66개 중 23개가 그랬고, 그 절반은 반대편 골을
  건너편에서 보여주고 있었다. 관중은 공 근처에 있으므로 가장 크게 들은 카메라가
  가까운 카메라다. ROI 가 답을 낸 43개로 대조하니 소리로 고른 답이 **70%**
  일치했다(기존 방식은 26%). 우선순위는 ROI → `main_cam`(사용자가 지정했다면)
  → 소리 순이다.
- 카메라 간 소리 비교를 **k 로 정규화**해서 한다. 기종·게인·거리·잡음 바닥이
  다른 두 마이크의 원시 dB 를 비교하면 "프리앰프가 더 뜨거운 쪽"이 이긴다.
- 클립 라벨이 **실제 시작 시각**을 보여준다. 이전에는 탐지가 제안한 앵커(`T`)를
  그렸기 때문에, 시작점을 끌어도 숫자가 그대로였고 다르게 자른 두 클립이 같은
  시각으로 보였다. 앵커는 인스펙터에 `소리 8:37` 로 따로 남는다.

### 수정
- 골망 히트를 함성보다 최대 11초 앞까지 이 골로 인정하던 문제. 관중은 공을 한
  박자 뒤에 따라오므로 그만큼 앞선 히트는 직전 플레이의 네트 움직임이고, 그걸
  집으면 클립이 골을 벗어나고 앵글까지 그쪽으로 끌려간다. 눈으로 확인한 앵글
  오답 3건이 모두 9.4 / 10.1 / 11.6초짜리였고, 정상 보정의 중앙값은 3.8초였다.
  `locate.max_lead_sec`(기본 9초)로 설정에 노출했다 — 구장·카메라 배치마다 다를
  값이라 상수로 묻어둘 것이 아니다.
- 인접한 클립이 같은 구간을 두 번 보여주던 문제. 클립의 **끝**만 다음 클립을
  피해 잘리고 **시작**은 앞 클립과 무관하게 정해져서, 앵커가 가까울 때 뒤 클립의
  도입부가 앞 클립을 파고들었다. 실측한 경기에서 12쌍이 겹쳤고 최악은 10초 중
  8초가 같았다. 앵커 간격이 고르지 않은 건 `_refine_anchor` 가 골망 모션 지점으로
  최대 11초까지 앵커를 당기기 때문이다 — 피크로는 min_gap 을 통과한 둘이 앵커로는
  몇 초 차이가 된다.

### 추가
- **편집 저장·불러오기.** 포함/제외·복제·분할·조정한 시작과 끝이
  `data/_gui/edit_<게임>.json` 에 자동 저장되고, 다시 열면 복원된다. 탐지 캐시와
  별도라 다시 분석해도 편집이 날아가지 않는다. 이전에는 브라우저 탭에만 있어서
  닫으면 사라졌다.

## [0.1.4] - 2026-08-13

### 변경
- 앱 아이콘 원본을 새 디자인으로 교체하고 macOS `.icns`와 Windows `.ico`를
  다시 생성했다.

### 수정
- 일부 최신 macOS에서 `iconutil`이 정상 아이콘 세트를 거부할 때도 Pillow로
  `.icns`를 생성할 수 있도록 아이콘 생성 스크립트에 대체 경로를 추가했다.

## [0.1.3] - 2026-08-13

### 수정
- 설치판이 ffmpeg 를 못 찾던 문제. Finder 로 띄운 앱은 셸의 PATH 를 물려받지
  않고 `/usr/bin:/bin` 만 받는데, Homebrew 는 `/opt/homebrew/bin` 에 깔린다.
  그래서 터미널에서 멀쩡히 도는 ffmpeg 를 두고 "설치해 주세요" 라고 했고,
  `ffmpeg 설치하기` 를 눌러도 "Homebrew 가 없습니다" 로 막혔다 — brew 를 찾는
  코드도 같은 PATH 를 보고 있었다. 표준 설치 위치까지 함께 찾는다.

### 변경
- 앱 아이콘 추가 (macOS `.icns` · Windows `.ico`). 원본과 생성 스크립트는
  `packaging/icon/` 과 `packaging/make_icons.py` 에 있다.
- 첫 화면에서 `AI 골 라벨링` 점검 카드 제거. 이 카메라 배치에서 쓸 만하지
  않은 기능을 준비물처럼 띄우고 있었다.

## [0.1.2] - 2026-08-13

### 추가
- macOS 빌드가 Developer ID 로 서명·공증된다 — 이제 그냥 열린다. macOS 15 가
  Control-클릭 우회를 없애서, 미서명 앱은 시스템 설정을 파고들어야 했다.
  `packaging/build.py --sign --notarize`, 태그를 밀면 CI 가 같은 일을 한다.
- `anthropic` 을 `vision` extra 로 선언. 선언된 적이 없어서 README 대로 설치한
  뒤 골 라벨링을 켜면 ModuleNotFoundError 가 났다.
- 첫 화면에서 `ffmpeg 설치하기` 버튼 — 각 OS 패키지 관리자로 한 번에 설치.
- 설정 화면의 세부 설정 10종 — `config.yaml` 을 열지 않고 탐지 민감도·장면
  길이·출력 크기 등을 조절. 저장해도 파일의 주석과 정렬은 그대로 유지된다.
- 렌더 남은 시간 표시, 완료 후 `결과 폴더 열기`.
- 단축키 패널 (`?`).

### 변경
- 오픈소스 공개 정리: 개인 데이터(`net_rois.json`, `data/train_events.jsonl`)를
  추적에서 제외하고 예시 파일로 대체. AGPL 의존성(ultralytics)을 선택 extra 로
  분리하고 해당 모듈에 라이선스 경계를 명시.

### 보안
- `SECURITY.md` — 취약점 비공개 신고 절차와, 로컬 도구로서 무엇이 취약점
  범위에 드는지. 포트를 여는 프로그램을 배포하면 언젠가 필요해진다.
- 로컬 서버가 자기 페이지에서 온 요청만 처리한다. 이전에는 `127.0.0.1` 바인드에
  기대고 있었는데, 그건 다른 기계를 막을 뿐 다른 **탭**을 막지 못한다. 앱이 켜져
  있는 동안 사용자가 방문한 아무 사이트나 `/api/install_ffmpeg`(패키지 관리자
  실행)나 `/api/settings`(설정 덮어쓰기)를 호출할 수 있었고, DNS 리바인딩을 쓰면
  응답까지 읽혔다. `Host`·`Origin`·`Sec-Fetch-Site` 세 헤더를 확인해 막는다.
  두 헤더가 모두 없는 요청(curl, 스크립트)은 그대로 통과 — 웹페이지는 브라우저가
  이 헤더를 빼도록 만들 수 없다.

### 수정
- 원본을 옮긴 뒤 렌더하면 ffmpeg 명령줄이 그대로 노출되던 오류 → 원본을 먼저
  확인하고 어떤 파일이 없는지 안내.
- 골대 위치 탭이 저장된 ROI 를 "아직 안 그림" 으로 표시하던 문제 (파이프라인은
  파일명으로 찾고 있었는데 화면만 정확한 경로로 보고 있었다).
- 영상이 든 폴더가 따로 있는데도 빈 폴더를 기본 선택하던 문제.
- ffmpeg 9 에서 렌더가 통째로 실패하던 문제. `-vsync` 는 5.1 에서 밀려나 수년간
  경고만 내다가 9 에서 사라졌는데, 그때부터는 경고가 아니라 실패한 명령이다.
  사용자가 자기 ffmpeg 를 들고 오는 이상 한쪽 철자로 고정할 수 없어서, 설치된
  ffmpeg 에 한 번 물어보고 `-fps_mode` 와 `-vsync` 중 되는 쪽을 쓴다.
- 골 라벨링을 기능으로 광고하던 README — 개발에 쓴 카메라 배치에서 진짜 골
  5개 중 1개를 맞혔다. 켜볼 사람이 그 숫자를 먼저 보도록 옮겼다.
- 비전 판정이 "오검출을 걸러낸다" 고 적혀 있던 문서 — 실제로는 라벨만 기록하고
  클립을 지우지 않는다. 그대로 읽으면 사라질 리 없는 클립을 기다리게 된다.

## [0.1.1] - 2026-07-29

### 추가
- 첫 실행 환경 점검 — ffmpeg, 필수 필터, 하드웨어 인코더, 폴더 권한, 디스크.
- 영상 폴더 열기, 진행 흐름 안내.

### 변경
- 전문 용어 정리 (`골 ROI` → `골대 위치`, `in/out/컷` → `시작/끝/전환` 등).

### 수정
- Windows 콘솔(cp949)에서 앱이 첫 출력 줄에 죽던 문제.

## [0.1.0] - 2026-07-29

첫 설치판 (macOS / Windows).

### 추가
- macOS `.app` / Windows `.exe` 빌드와 GitHub Actions 자동 빌드·릴리스.
- 3개 OS 에서 테스트를 도는 CI.

### 변경
- `librosa` 제거 — 오디오 디코딩·RMS 를 ffmpeg + numpy 로 직접 처리. 결과는
  동일(피크 시각 일치, dB 오차 5e-6 이하)하고 디코딩은 약 12배 빠르다.
  numba/llvmlite/sklearn 등 약 215MB 의존성이 빠졌다.

### 수정
- Windows 에서 concat 경로의 백슬래시가 이스케이프로 해석되어 렌더가 실패하던 문제.
- 텍스트 파일 입출력의 인코딩 미지정 (한국어 Windows 에서 cp949 로 깨짐).
- 하드웨어 인코더가 videotoolbox 만 지원되던 문제 → nvenc/qsv/amf 추가.
